#include "main.h"

/**
 * _abs - Entry point to check code
 * @r - integer to be input(ed
 * Description - This functions return the absolute value of a number
 * Return: return absolute value of number /integer r
 */

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}

}
